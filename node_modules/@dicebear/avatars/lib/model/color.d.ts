import ColorCollection from './color/collection';
export interface ColorInterface {
    alpha: number;
    hex: string;
    rgb: number[];
    rgba: number[];
    hsv: number[];
}
export default class Color implements ColorInterface {
    static collection: typeof ColorCollection;
    alpha: number;
    private color;
    constructor(color?: string);
    rgb: number[];
    rgba: number[];
    hsv: number[];
    hex: string;
    private rgbToHex(rgb);
    private hexToRgb(hex);
    private rgbToHsv(rgb);
    private hsvToRgb(hsv);
}
